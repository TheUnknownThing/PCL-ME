# Settings Wiring TODO

Audit scope: settings and instance-setup UI in `PCL.Frontend.Avalonia` that are currently not wired to an actual backend behavior, are only partially wired, or still rely on placeholder/simplified logic that should be updated for full cross-platform support.

## Unwired UI controls

- [ ] Wire `UpgradePartialAuthlib` in the Game Manage settings card. The UI binds `IsChecked="{Binding UpgradePartialAuthlib}"`, but there is no corresponding view-model property, persistence path, or downstream consumer. Evidence: `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/SetupGameManageShellRightPaneView.axaml:132`, repo search only finds localization plus this XAML binding.
- [ ] Replace the Game Manage `TargetFolder` row with a real setting or remove it. The row is currently only two `TextBlock`s with no bound value, picker, or command. Evidence: `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/SetupGameManageShellRightPaneView.axaml:104`.

## Persisted settings with no backend effect

- [ ] Either implement or remove launcher update mode handling. `SelectedUpdateModeIndex` is loaded and persisted through `SystemSystemUpdate`, but update checks only use `SelectedUpdateChannelIndex`. Evidence: `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.LaunchUpdate.cs:56`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SetupComposition.cs:447`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SurfaceActions.Updates.cs:12`.
- [ ] Either implement or remove `NotifyReleaseUpdates` and `NotifySnapshotUpdates`. Both settings are loaded and saved, but there is no notification or update workflow consuming them. Evidence: `PCL.Frontend.Avalonia/Workflows/FrontendSetupCompositionService.cs:206`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SetupComposition.cs:559`, repo search shows no downstream use outside composition/persistence.
- [ ] Either implement or remove `OptimizeMemoryBeforeLaunch` / `LaunchArgumentRam`. The setting is loaded and persisted, but launch memory resolution only looks at `LaunchRamType` and `LaunchRamCustom`. Evidence: `PCL.Frontend.Avalonia/Workflows/FrontendSetupCompositionService.cs:177`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SetupComposition.cs:487`, `PCL.Frontend.Avalonia/Workflows/FrontendLaunchCompositionService.Arguments.cs:511`.
- [ ] Either implement or remove `RequireDedicatedGpu` / `LaunchAdvanceGraphicCard`. The value is loaded and persisted, but no launch or runtime path applies it. Evidence: `PCL.Frontend.Avalonia/Workflows/FrontendSetupCompositionService.cs:188`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SetupComposition.cs:520`, repo search shows no consumer outside composition/persistence.
- [ ] Either implement or remove instance memory optimization mode `VersionRamOptimize`. The selector is shown, loaded, and persisted, but it does not affect launch composition. Evidence: `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/Sections/InstanceSetupMemorySection.axaml:70`, `PCL.Frontend.Avalonia/Workflows/FrontendInstanceCompositionService.Setup.cs:74`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.InstanceComposition.cs:254`, repo search shows no downstream use.
- [ ] Either implement or remove instance auth register URL `VersionServerAuthRegister`. The field is shown, loaded, and persisted, but it is not used by auth flow, profile creation, or launch prechecks. Evidence: `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/Sections/InstanceSetupServerSection.axaml:55`, `PCL.Frontend.Avalonia/Workflows/FrontendInstanceCompositionService.Setup.cs:87`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.InstanceComposition.cs:263`.

## Partially wired or placeholder logic

- [ ] Rework settings import/export into a real round-trip format. Export writes both shared and local config files into a timestamped directory, but import only accepts a single `*.json` file and copies it into `SharedConfigPath`, leaving local settings out of sync. This is a simplified implementation rather than a full restore path. Evidence: `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SurfaceActions.LogsAndSettings.cs:103`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SurfaceActions.LogsAndSettings.cs:119`.
- [ ] Decide whether `InstanceServerAuthName` is metadata or real auth configuration. It is persisted and shown in the UI, but the only confirmed behavior is using it as display text when opening the authlib profile editor. Evidence: `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/Sections/InstanceSetupServerSection.axaml:68`, `PCL.Frontend.Avalonia/Workflows/FrontendInstanceCompositionService.Setup.cs:88`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.SurfaceActions.cs:132`.
- [ ] Move `VersionServerLoginLock` from a UI-only lock to real enforcement, or relabel it. Today it only disables local editing controls and shows a warning; launch/auth composition does not treat it as a security or policy constraint. Evidence: `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.InstanceSetup.cs:223`, `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/Sections/InstanceSetupServerSection.axaml:30`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.InstanceComposition.cs:324`, repo search shows no launch/precheck consumer.
- [ ] Replace the unsupported theme-switch action with a real platform-aware flow. The notice button currently opens the repository URL rather than resolving a proper snapshot/nightly/update path per platform. This looks like a placeholder migration step. Evidence: `PCL.Frontend.Avalonia/Desktop/ShellViews/Right/SetupUiShellRightPaneView.axaml:179`, `PCL.Frontend.Avalonia/ViewModels/FrontendShellViewModel.Construction.cs:407`.
