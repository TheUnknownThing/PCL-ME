<UserControl
    x:Class="PCL.Frontend.Avalonia.Desktop.Panes.Right.Sections.InstanceExportOptionGroupSection"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel Margin="0,0,0,2">
    <CheckBox
        MinHeight="26"
        Foreground="{DynamicResource ColorBrush1}"
        IsChecked="{Binding Header.IsChecked}">
      <TextBlock TextWrapping="Wrap">
        <Run Text="{Binding Header.Title}" />
        <Run Foreground="{DynamicResource ColorBrushEntrySecondaryIdle}" Text="{Binding Header.InlineDescription}" />
      </TextBlock>
    </CheckBox>

    <ItemsControl ItemsSource="{Binding Children}" IsVisible="{Binding Header.IsChecked, FallbackValue=False}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <CheckBox
              MinHeight="26"
              Margin="30,0,0,0"
              Foreground="{DynamicResource ColorBrush1}"
              IsChecked="{Binding IsChecked}">
            <TextBlock TextWrapping="Wrap">
              <Run Text="{Binding Title}" />
              <Run Foreground="{DynamicResource ColorBrushEntrySecondaryIdle}" Text="{Binding InlineDescription}" />
            </TextBlock>
          </CheckBox>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
</UserControl>
