<UserControl
    x:Class="PCL.Frontend.Avalonia.Desktop.ShellViews.Right.Sections.InstanceOverviewShortcutsSection"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:PCL.Frontend.Avalonia.Desktop.Controls">
  <controls:PclCard Header="{Binding InstanceOverviewShortcutsHeaderText}" ContentMargin="20,40,20,18">
    <controls:PclCard.CardContent>
      <Grid ColumnDefinitions="Auto,Auto,Auto">
        <controls:PclButton
            Grid.Column="0"
            MinWidth="140"
            Margin="0,0,20,0"
            Command="{Binding OpenInstanceFolderCommand}"
            Text="{Binding InstanceOverviewOpenInstanceFolderText}" />
        <controls:PclButton
            Grid.Column="1"
            MinWidth="140"
            Margin="0,0,20,0"
            Command="{Binding OpenInstanceSavesFolderCommand}"
            Text="{Binding InstanceOverviewOpenSavesFolderText}" />
        <controls:PclButton
            Grid.Column="2"
            MinWidth="140"
            Command="{Binding OpenInstanceModsFolderCommand}"
            Text="{Binding InstanceOverviewOpenModsFolderText}" />
      </Grid>
    </controls:PclCard.CardContent>
  </controls:PclCard>
</UserControl>
